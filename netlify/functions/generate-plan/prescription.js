'use strict';

const engine = require('./engine.js');

module.exports = {
    prescribeForExercise: engine.prescribeForExercise,
    estimateSessionDurationSeconds: engine.estimateSessionDurationSeconds
};
